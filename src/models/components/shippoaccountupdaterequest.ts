/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

export type ShippoAccountUpdateRequest = {
    email: string;
    firstName: string;
    lastName: string;
    companyName: string;
};

/** @internal */
export namespace ShippoAccountUpdateRequest$ {
    export const inboundSchema: z.ZodType<ShippoAccountUpdateRequest, z.ZodTypeDef, unknown> = z
        .object({
            email: z.string(),
            first_name: z.string(),
            last_name: z.string(),
            company_name: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                first_name: "firstName",
                last_name: "lastName",
                company_name: "companyName",
            });
        });

    export type Outbound = {
        email: string;
        first_name: string;
        last_name: string;
        company_name: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, ShippoAccountUpdateRequest> = z
        .object({
            email: z.string(),
            firstName: z.string(),
            lastName: z.string(),
            companyName: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                firstName: "first_name",
                lastName: "last_name",
                companyName: "company_name",
            });
        });
}
