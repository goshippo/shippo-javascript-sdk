/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Specify the invoice number field on the label (FedEx and UPS only).
 */
export type InvoiceNumber = {
  /**
   * Custom prefix for invoice number field (ZPL labels only). Up to 11 characters, including trailing
   *
   * @remarks
   * spaces. Empty string indicates removal of default prefix. To use the default prefix, do not include
   * this property.
   */
  prefix?: string | undefined;
  /**
   * Optional text to be printed on the shipping label for invoice number. Up to 40 characters. If
   *
   * @remarks
   * provided, this will be used on the label instead of shipment.customs_declaration.invoice.
   */
  value?: string | undefined;
  /**
   * Order UPS reference fields are printed on ZPL labels. For UPS shipments, if you choose to set `ref_sort` for one reference, you must set `ref_sort` for all other supported UPS references using unique integers.
   */
  refSort?: number | undefined;
};

/** @internal */
export const InvoiceNumber$inboundSchema: z.ZodMiniType<
  InvoiceNumber,
  unknown
> = z.pipe(
  z.object({
    prefix: z.optional(z.string()),
    value: z.optional(z.string()),
    ref_sort: z.optional(z.int()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "ref_sort": "refSort",
    });
  }),
);
/** @internal */
export type InvoiceNumber$Outbound = {
  prefix?: string | undefined;
  value?: string | undefined;
  ref_sort?: number | undefined;
};

/** @internal */
export const InvoiceNumber$outboundSchema: z.ZodMiniType<
  InvoiceNumber$Outbound,
  InvoiceNumber
> = z.pipe(
  z.object({
    prefix: z.optional(z.string()),
    value: z.optional(z.string()),
    refSort: z.optional(z.int()),
  }),
  z.transform((v) => {
    return remap$(v, {
      refSort: "ref_sort",
    });
  }),
);

export function invoiceNumberToJSON(invoiceNumber: InvoiceNumber): string {
  return JSON.stringify(InvoiceNumber$outboundSchema.parse(invoiceNumber));
}
export function invoiceNumberFromJSON(
  jsonString: string,
): SafeParseResult<InvoiceNumber, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InvoiceNumber$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InvoiceNumber' from JSON`,
  );
}
