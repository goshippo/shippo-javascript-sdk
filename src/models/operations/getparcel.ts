/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as z from "zod";

export type GetParcelRequest = {
    /**
     * Object ID of the parcel
     */
    parcelId: string;
};

/** @internal */
export namespace GetParcelRequest$ {
    export type Inbound = {
        ParcelId: string;
    };

    export const inboundSchema: z.ZodType<GetParcelRequest, z.ZodTypeDef, Inbound> = z
        .object({
            ParcelId: z.string(),
        })
        .transform((v) => {
            return {
                parcelId: v.ParcelId,
            };
        });

    export type Outbound = {
        ParcelId: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, GetParcelRequest> = z
        .object({
            parcelId: z.string(),
        })
        .transform((v) => {
            return {
                ParcelId: v.parcelId,
            };
        });
}
